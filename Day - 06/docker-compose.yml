version: "3.9"

services:

  flask:

    build:
      context: ./app

    image: devsecops-flask:v3

    container_name: flask-v3

    env_file:
      - ./app/.env.example

    ports:
      - "5000:5000"

    restart: unless-stopped
