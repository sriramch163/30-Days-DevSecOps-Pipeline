version: "3.9"

services:

  flask:

    build: ./app

    container_name: flask-network

    networks:
      - dev-network

    ports:
      - "5000:5000"

networks:

  dev-network:
    driver: bridge
